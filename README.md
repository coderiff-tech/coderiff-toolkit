# coderiff-toolkit
Dotnet Tool

Install globally
```
dotnet tool install -g Coderiff.Cli
```

The package should be publicly available at https://nuget.pkg.github.com/coderiff-tech/index.json
Authentication is granted due to the credentials configured at NuGet.Config, make sure this file is available
from the directory where the tool is installed.